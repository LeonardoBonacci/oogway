version: '3'

services:
 
  integration-service:
    environment:
      SPRING_PROFILES_ACTIVE: it
    image: integration-service #avoid pulling from docker hub by defining local name
    ports:
      - 3334:8080
