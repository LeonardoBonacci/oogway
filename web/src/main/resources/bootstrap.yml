spring:
  application:
    name: web-service
  cloud:
    config:
      uri: http://localhost:8888
      label: ${CLOUD_CONFIG_FIND_LABEL:master}
      fail-fast: true
  freemarker: 
    enabled: false
  profiles: 
    active: dev