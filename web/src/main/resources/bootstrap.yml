spring:
  application:
    name: web-service
  cloud:
    config:
      uri: http://localhost:8888
      label: ${cloud.config.find.label:master}
      fail-fast: true
  freemarker: 
    enabled: false
  profiles: 
    active: dev