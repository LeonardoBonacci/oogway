spring:
  application:
    name: web
  cloud:
    config:
      uri: http://localhost:8888
      fail-fast: true
  freemarker: 
    enabled: false
  profiles: 
    active: dev
    
logstash:
  host: localhost
  port: 5000    